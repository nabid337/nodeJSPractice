<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
  
</head>
<body>
  May Node and Express be with you.

  <div>
  <ul class="quotes">
      <% for(var i=0; i<quotes.length; i++) {%>
        <li class="quote">
          <span><%= quotes[i].quoteBy %></span>
          <span><%= quotes[i].quoteMsg %></span>
        </li>
      <% } %>
      </ul>
  </div>

  <div>

  <form action="/quotes" method="POST">
    <input type="text" placeholder="name" name="quote_by">
    <input type="text" placeholder="quote" name="quote_msg">
    <button type="submit">Submit</button>
  </form>
  </div>

  <div>
    <h2>Replace last quote written by Master Yoda with a quote written by Darth Vadar</h2>
    <button id="update"> Darth Vadar invades! </button>
  </div>
  <div>
      <a type="button" href="/logout">Logout</a>
      </div>
  <script src="put.js"></script>
</body>
</html>